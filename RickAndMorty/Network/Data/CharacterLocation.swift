import Foundation

struct CharacterLocation: Codable, Hashable {

    /// Name to the character's location.
    let name: String

    /// Link (url) to the character's location.
    let url: String
}
